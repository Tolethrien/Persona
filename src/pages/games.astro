---
import Layout from "../layouts/Layout.astro";
import { getCollection } from "astro:content";
import ProjectsReactSection from "../components/react/projectSectionReact/projectSectionReact";
const projects = await getCollection("games");
---

<Layout pageName="Games">
  <ProjectsReactSection client:visible content={projects} />
</Layout>
